* also add command aliases a for add, g for game, m for manual, a for auto.
and also add "fast commands" which allow user to do fast cmds
like instead of add game manual or a g m they can just do agm

- implement code for any client (in core) to create game presets with constants, and if it doesnt exist file then just create, else leave it (on github we will update the app that way we update the presets)

- impl cmd: add auto game from preset